class Solution {
public:
    void wiggleSort(vector<int>& nums) {
        vector<int> arr = nums;
        sort(arr.begin(), arr.end());
        int n = nums.size();
        int mid = (n - 1) / 2;
        int end = n - 1;
        for(int i = 0; i < n; i++) {
            if(i % 2 == 0)
                nums[i] = arr[mid--];
            else
                nums[i] = arr[end--];
        }
    }
};
